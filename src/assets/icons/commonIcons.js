import { BiSearch } from 'react-icons/bi';
import { BsPerson } from 'react-icons/bs';

const Search = () => {
    return <BiSearch />;
};
const User = () => {
    return <BsPerson />;
};

const Icon = { Search, User };

export default Icon;
